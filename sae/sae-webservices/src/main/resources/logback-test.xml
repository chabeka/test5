<configuration>
   <!--
      Insert the current time formatted as "yyyyMMdd'T'HHmmss" under the
      key "bySecond" into the logger context. This value will be
      available to all subsequent configuration elements.
   -->
   <timestamp key="bySecond" datePattern="yyyyMMdd'T'HHmmss" />

   <property resource="saeLogVariables.properties" />

   <appender name="STDOUT" class="ch.qos.logback.core.ConsoleAppender">
      <encoder>
         <pattern>%d{HH:mm:ss.SSS} %-5level %logger
            [%X{log_contexte_uuid}] - %msg%n</pattern>
      </encoder>
   </appender>

   <appender name="FILE" class="ch.qos.logback.core.FileAppender">
      <file>${USER_HOME}/sae-serviceweb.log</file>
      <append>false</append>
      <!--
         <rollingPolicy
         class="ch.qos.logback.core.rolling.FixedWindowRollingPolicy">
         <fileNamePattern>sae-service.%i.log.zip</fileNamePattern>
         <minIndex>1</minIndex> <maxIndex>3</maxIndex> </rollingPolicy>

         <triggeringPolicy
         class="ch.qos.logback.core.rolling.SizeBasedTriggeringPolicy">
         <maxFileSize>5KB</maxFileSize> </triggeringPolicy>
      -->
      <encoder>
         <pattern>%d{HH:mm:ss.SSS} %-5level %logger
            [%X{log_contexte_uuid}] - %msg%n</pattern>
      </encoder>
   </appender>

   <!--
      <logger name="fr.urssaf.image.sae" level="debug"> <appender-ref
      ref="FILE" /> </logger>
   -->
   <root level="debug">
      <appender-ref ref="FILE" />
   </root>
</configuration>