<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
   xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
   <parent>
      <artifactId>commons-webservice-custom</artifactId>
      <groupId>fr.urssaf.image.commons</groupId>
      <version>0.0.1-SNAPSHOT</version>
   </parent>
   <modelVersion>4.0.0</modelVersion>
   <groupId>fr.urssaf.image.commons</groupId>
   <artifactId>commons-webservice-exemple</artifactId>
   <version>0.0.1-SNAPSHOT</version>
   <name>commons-webservice-exemple</name>

   <properties>
      <server>src/test/resources</server>
      <wsdl.document>/DocServiceDocument.xml</wsdl.document>
      <wsdl.rpc.literal>/DocServiceRpcLiteral.xml</wsdl.rpc.literal>
      <wsdl.rpc.encoded>/DocServiceRpcEncoded.xml</wsdl.rpc.encoded>
   </properties>

   <profiles>
      <profile>
         <id>generateWebService</id>

         <build>
            <plugins>

               <plugin>
                  <groupId>org.apache.cxf</groupId>
                  <artifactId>cxf-codegen-plugin</artifactId>
                  <version>${cxf.version}</version>
                  <executions>
                     <execution>
                        <id>generate-sources</id>
                        <phase>generate-sources</phase>
                        <configuration>
                           <sourceRoot>${basedir}/src/main/java</sourceRoot>
                           <wsdlOptions>
                              <wsdlOption>
                                 <wsdl>${server}${wsdl.document}</wsdl>
                                 <extraargs>
                                    <extraarg>-verbose</extraarg>
                                 </extraargs>
                                 <packagenames>
                                    <packagename>fr.urssaf.image.commons.webservice.exemple.document.modele</packagename>
                                 </packagenames>
                              </wsdlOption>
                              <wsdlOption>
                                 <wsdl>${server}${wsdl.rpc.encoded}</wsdl>
                                 <extraargs>
                                    <extraarg>-verbose</extraarg>
                                 </extraargs>
                                 <packagenames>
                                    <packagename>fr.urssaf.image.commons.webservice.exemple.rpc.encoded.modele</packagename>
                                 </packagenames>
                              </wsdlOption>
                              <wsdlOption>
                                 <wsdl>${server}${wsdl.rpc.literal}</wsdl>
                                 <extraargs>
                                    <extraarg>-verbose</extraarg>
                                 </extraargs>
                                 <packagenames>
                                    <packagename>fr.urssaf.image.commons.webservice.exemple.rpc.literal.modele</packagename>
                                 </packagenames>
                              </wsdlOption>
                           </wsdlOptions>
                        </configuration>
                        <goals>
                           <goal>wsdl2java</goal>
                        </goals>
                     </execution>
                  </executions>
               </plugin>
            </plugins>
         </build>
      </profile>
   </profiles>
   
   <build>
      <plugins>
         <plugin>
            <groupId>org.apache.maven.plugins</groupId>
            <artifactId>maven-surefire-plugin</artifactId>
            <version>2.5</version>
            <configuration>
               <skipTests>true</skipTests>
            </configuration>
         </plugin>

      </plugins>
   </build>

   <dependencies>

      <dependency>
         <groupId>org.apache.cxf</groupId>
         <artifactId>cxf-rt-transports-http</artifactId>
         <version>${cxf.version}</version>
      </dependency>

      <dependency>
         <groupId>org.apache.cxf</groupId>
         <artifactId>cxf-rt-frontend-jaxws</artifactId>
         <version>${cxf.version}</version>
      </dependency>

      <dependency>
         <groupId>org.springframework</groupId>
         <artifactId>spring-test</artifactId>
         <version>${org.springframework.version}</version>
         <scope>test</scope>
      </dependency>

   </dependencies>

</project>