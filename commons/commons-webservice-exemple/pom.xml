<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
	<parent>
		<artifactId>commons-webservice-parent</artifactId>
		<groupId>fr.urssaf.image.commons</groupId>
		<version>0.0.1-SNAPSHOT</version>
	</parent>
	<modelVersion>4.0.0</modelVersion>
	<groupId>fr.urssaf.image.commons</groupId>
	<artifactId>commons-webservice-exemple</artifactId>
	<version>0.0.1-SNAPSHOT</version>
   <name>commons-webservice-exemple</name>

	<build>
		<plugins>
         <!--
         ATTENTION : Il faut mettre en commentaire
         le plugin de génération des sources avec CXF
         avant de commiter, car en intégration continue, 
         le goal generate-sources est appelé par le goa
         compile, et donc regénère à chaque fois les sources
         à partir du WSDL. Ceci est fortement déconseillé.
         De plus, on peut avoir un plantage si le WSDL n'est
         pas ou plus accessible à partir de la plateforme 
         d'intégration continue.
         -->
			<!-- 
         <plugin>
				<groupId>org.apache.cxf</groupId>
				<artifactId>cxf-codegen-plugin</artifactId>
				<version>2.2.8</version>
				<executions>
					<execution>
						<id>generate-sources</id>
						<phase>generate-sources</phase>
						<configuration>
							<sourceRoot>${basedir}/src/main/java</sourceRoot>
							<wsdlOptions>
								<wsdlOption>
									<wsdl>http://localhost:9999/DocServiceDocument?WSDL</wsdl>
									<packagenames>
										<packagename>fr.urssaf.image.commons.webservice.exemple.document.modele</packagename>
									</packagenames>
								</wsdlOption>
								<wsdlOption>
									<wsdl>http://localhost:9999/DocServiceRpcEncoded?WSDL</wsdl>
									<packagenames>
										<packagename>fr.urssaf.image.commons.webservice.exemple.rpc.encoded.modele</packagename>
									</packagenames>
								</wsdlOption>
								<wsdlOption>
									<wsdl>http://localhost:9999/DocServiceRpcLiteral?WSDL</wsdl>
									<packagenames>
										<packagename>fr.urssaf.image.commons.webservice.exemple.rpc.literal.modele</packagename>
									</packagenames>
								</wsdlOption>
							</wsdlOptions>
						</configuration>
						<goals>
							<goal>wsdl2java</goal>
						</goals>
					</execution>
				</executions>
			</plugin>
         -->

			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-surefire-plugin</artifactId>
				<version>2.5</version>
				<configuration>
					<skipTests>true</skipTests>
				</configuration>
			</plugin>

		</plugins>
	</build>

	<dependencies>

		<dependency>
			<groupId>fr.urssaf.image.commons</groupId>
			<artifactId>commons-webservice</artifactId>
			<version>0.0.1-SNAPSHOT</version>
		</dependency>

		<dependency>
			<groupId>org.apache.cxf</groupId>
			<artifactId>cxf-tools-wsdlto-frontend-jaxws</artifactId>
			<version>2.2.8</version>
			<scope>test</scope>
		</dependency>

		<dependency>
			<groupId>org.apache.cxf</groupId>
			<artifactId>cxf-tools-wsdlto-databinding-jaxb</artifactId>
			<version>2.2.8</version>
			<scope>test</scope>
		</dependency>
		
		<dependency>
			<groupId>org.springframework</groupId>
			<artifactId>spring-test</artifactId>
			<version>2.5.6</version>
			<scope>test</scope>
		</dependency>
		

	</dependencies>

</project>